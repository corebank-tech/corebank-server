# DB 데이터 마이그레이션 및 시딩 가이드라인

본 문서는 Flyway 마이그레이션 스크립트(`V__`, `R__`)와 시딩 데이터(`seed`) 파일의 파일명 규칙, 역할, 그리고 배포 환경에 따른 분리 기준에 집중한 가이드라인입니다.

## 1. V__ (Versioned) 파일: DDL 전용
* **목적**: 테이블 생성, 컬럼 변경, 제약조건 추가 등 **DB 스키마 구조(DDL)** 변경 전용
* **파일명 규칙**: **`V{yyyyMMddHHmm}__{설명}.sql`** (예: `V202608051430__add_account_nickname.sql`)
  * V1, V2와 같은 순번은 동시 개발 시 충돌하므로 반드시 **타임스탬프** 방식을 사용합니다.
* **실행 환경**: `local`, `test`, `prod` 등 **모든 환경**
* **주의사항**:
  * 한 번 적용된 `V__` 파일은 **절대 수정해서는 안 됩니다.** (수정 시 마이그레이션 체크섬 불일치로 부팅 실패)
  * 테스트용 더미 데이터나 초기 시드 데이터를 `V__` 파일에 `INSERT` 하지 마세요. 데이터 수정 시 파일을 건드릴 수 없어 관리가 어렵고, 운영(`prod`) 환경에 불필요한 데이터가 유입될 위험이 큽니다.

## 2. R__ (Repeatable) 파일: 전 환경 필수 마스터 데이터 시딩
* **목적**: 시스템 구동 및 운영에 필수적인 **마스터 데이터**(예: 시스템 관리자 계정, 공통 코드, 약관 데이터, 상품 마스터 등) 시딩
* **파일명 규칙**: **`R__{설명}.sql`** (예: `R__system_common_codes.sql`)
* **실행 환경**: `local`, `test`, `prod` 등 **모든 환경**
* **주의사항**:
  * **운영(`prod`) 환경에도 반드시 존재해야 하는 데이터**만 `R__` 스크립트에 작성합니다.
  * 파일 내용이 변경될 때마다 Flyway가 이를 감지하여 다시 실행하므로, **반드시 멱등성(Idempotency)**을 보장해야 합니다.
  * 중복 실행 시 기본키 충돌 에러가 나지 않도록 단순 `INSERT` 대신 **`INSERT INTO ... ON DUPLICATE KEY UPDATE`** 구문으로 작성하는 것이 팀 컨벤션입니다.

## 3. local-demo-data.sql: 로컬/테스트 전용 더미 데이터
* **위치**: `src/main/resources/db/seed/local-demo-data.sql` (Flyway 마이그레이션 경로 밖)
* **목적**: 로컬 기능 테스트 및 시연용으로 필요한 **가짜/더미 데이터**(예: 테스트용 '홍길동' 고객 계정, 임의의 거래 내역 등) 시딩
* **실행 환경**: `local` 및 일부 `test` 환경 (운영 `prod`에서는 절대 실행되지 않음)
* **주의사항**:
  * Flyway가 자동으로 읽어가는 `db/migration/` 폴더에 넣지 마십시오.
  * 별도의 스크립트 실행기나 개발/테스트 프로필 전용 시딩 로직(예: Spring Batch, `data.sql` 로딩 설정 등)을 통해서만 주입되어야 합니다.
